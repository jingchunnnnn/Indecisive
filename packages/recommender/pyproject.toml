[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "indecisive-recommender"
version = "0.1.0"
description = "Shared recommendation utilities for Indecisive."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
